<!-- templates/track_bus.html -->
<!DOCTYPE html>
<html>
<head>
    <title>Track Bus</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.3/dist/leaflet.css" />
    <style>
        #map {
            height: 500px;
            width: 100%;
        }
    </style>
</head>
<body>
    <h2>Live Tracking: Bus {{ bus.bus_no }}</h2>
    <div id="map"></div>

    <script src="https://unpkg.com/leaflet@1.9.3/dist/leaflet.js"></script>
    <script>
        let map = L.map('map').setView([{{ bus.current_latitude }}, {{ bus.current_longitude }}], 15);
        let marker = L.marker([{{ bus.current_latitude }}, {{ bus.current_longitude }}]).addTo(map);

        L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
            attribution: '© OpenStreetMap contributors'
        }).addTo(map);

        function updateBusLocation() {
            fetch('/api/get-location/{{ bus.bus_no }}/')
                .then(res => res.json())
                .then(data => {
                    marker.setLatLng([data.latitude, data.longitude]);
                    map.setView([data.latitude, data.longitude]);
                });
        }

        setInterval(updateBusLocation, 5000);  // Update every 5 seconds
    </script>
</body>
</html>
